# YumPair

A Next.js and Tailwind CSS hero page for YumPair, an AI-powered meal and drink pairing landing page.

## Getting Started

Install dependencies and run the development server:

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000) in your browser.
